import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function ImageGenerationTab() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>生图</CardTitle>
      </CardHeader>
      <CardContent>
        <p>这里是最终生成和预览漫画的界面。</p>
      </CardContent>
    </Card>
  );
}
